<div class="row">
  <div id="newsfeed" class="col-lg-6">
    <h4><%= fa_icon "newspaper-o" %> ΠΡΟΣΦΑΤΑ</h4>
    <div id="activities">
      <% if @activities.empty? %>
        <div class="well">
          <%= render('shared/no_resource', resources: 'Activities') %>
        </div>
      <% else %>
        <%= render_activities(@activities, layout: '/shared/activity') %>
      <% end %>
    </div>
    <%= render 'shared/paginate', resources: @activities %>
  </div>
  <div id="intro" class="col-lg-6">
    <h1>Teenager</h1>
    <p>H αίτηση για εγγραφή θα είναι διαθέσιμη πολύ σύντομα. Στα τέλη περίπου του καλοκαιριού θα ενεργοποιηθούν όλοι οι λογαριασμοί.</p>
  </div>
</div>